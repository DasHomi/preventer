package com.dashomi.preventer.enums;

public enum PreventSuspiciousBlockBreakingConfig {
    OFF, // disabled
    NORMAL, // prevents you from breaking suspicious blocks
    ENHANCED // prevents you from breaking blocks below suspicious blocks
}
